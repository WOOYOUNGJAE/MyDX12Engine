

void VS(float3 iPosL : POSITION, //input Signature, POSITION은 의미소(Semantic)
		float4 iColor : COLOR,
		out float4 oPosH : SV_POSITION,// SV(System Value) : 위치를 담음 출력성분이라는 것을 알림
		out float4 oColor : COLOR // SV가 아닌 출력 매개변수 의미소는 어떤 이름도 가능
)
{
    
}